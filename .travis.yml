language: cpp

install: true

script: make && make test

addons:
    apt:
        sources:
        - ubuntu-toolchain-r-test
        packages:
        - gcc-4.8
        - g++-4.8

env: CXX=g++-4.8 CC=gcc-4.8

